pipeline {
    agent {label 'Linux'}
    stages {
        stage('Checkout') {
            steps {
                echo 'Hello, Maven'
                echo 'bobo'
             }
        }
    }
}
                    







